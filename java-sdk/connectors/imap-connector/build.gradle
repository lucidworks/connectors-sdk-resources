dependencies {
  // Note: Fusion libraries are added by parent project.

  implementation 'com.sun.mail:javax.mail:1.6.1'

  testImplementation group: 'org.mockito', name: 'mockito-core', version: '2.23.4'
  testImplementation "com.lucidworks-schema:schema:${connectorsSDKVersion}"
}
